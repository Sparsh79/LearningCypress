platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: latest

inputs:
  - name: cypressGit
outputs:
  - name: output

run:
  path: /bin/bash
  args:
    - cypressGit/test-script.sh
