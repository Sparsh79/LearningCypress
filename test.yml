platform: linux

image_resource:
  type: docker-image
  source:
    repository: ddubson/cypress-e2e

inputs:
  - name: cypressAutomation
outputs:
  - name: npm-output

run:
  path: /bin/bash
  args:
    - cypressAutomation/ci/e2e.sh